experiment: uieb_lab_retrain_50k
mode: cold_gray
seed: 42
output_dir: outputs/uieb_lab_retrain_50k

data:
  image_size: 128
  num_workers: 0
  validation_preview_name: "464_img_"

model:
  base_channels: 32
  channel_mults: [1, 2, 4, 8]
  dropout: 0.0

diffusion:
  steps: 8

training:
  max_steps: 50000
  batch_size: 4
  grad_accum: 4
  learning_rate: 0.00002
  ema_decay: 0.995
  ema_update_every: 10
  amp: true
  log_every: 50
  validate_every: 1000
  save_every: 5000
  max_val_batches: 20
