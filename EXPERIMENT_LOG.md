# Experiment log

Record real experiments here. Do not treat smoke tests as scientific evidence.

| Date | Question | Code/config | Data | Command | Result | Decision |
|---|---|---|---|---|---|---|
| 2026-08-24 | Does the implementation satisfy endpoint, sampler, and resume invariants? | Initial 50k implementation | Synthetic tensors and 8 synthetic paired images | `pytest -q`; smoke train 2 steps; `--resume auto --max-steps 4`; `evaluate.py` | 4/4 tests passed; CLI resumed from step 2 and completed step 4; evaluation artifacts written | Implementation is ready for a 50-pair overfit test; these results are not scientific evidence. |
| 2026-08-24 | Can a fresh rented 4090 instance reach training with a short reproducible sequence? | GPUTA bootstrap scripts | Local UIEB copy; local CPU used only for preflight | `bash -n scripts/*.sh`; `ruff check .`; `pytest -q`; `tools/prepare_uieb.py --skip-download`; 2-step CPU smoke train | Shell/Python checks passed; UIEB validated as 890 pairs and split 720/80/90 | Push bootstrap changes; the instance must still pass `scripts/smoke_test_cuda.sh` before the paid 50k run. |
| 2026-08-25 | Can evaluation expose convergence and produce inspectable trajectories? | Evaluation report and horizontal visual update | Synthetic 2-step smoke run; layout tensors | `ruff check .`; `pytest -q`; smoke `train.py` then `evaluate.py` | 7/7 tests passed; evaluate produced a 2880×1620 curve report, summary JSON, 2048px comparison strips, horizontal trajectory strips, and metrics | Ready to rerun the real checkpoint at a fixed 512 crop; compare all later checkpoints/baselines with the same evaluation size. |
| 2026-08-25 | Can the fixed Test 90 use the same evaluation suite as prior FlowIE runs? | FlowIE-compatible extended evaluator | Test filenames compared with the previous FlowIE seed-42 list; synthetic paired metric test | `ruff check .`; `pytest -q` | Test filenames matched 90/90; 8/8 tests passed; evaluator writes the 14 legacy metrics plus Delta-E76 and trajectory monotonic as per-image CSV and mean Markdown | Rerun the real checkpoint with `--extended-metrics --extended-metric-size 256`; use identical spatial preprocessing for strict cross-model claims. |
| 2026-08-25 | Does Algorithm 2 improve over direct reconstruction from the same Cold model? | Same-checkpoint Direct-vs-Algorithm-2 evaluator | Two-image synthetic paired smoke set | 2-step CPU train; `evaluate.py`; `ruff check .`; `pytest -q` | Direct and Algorithm 2 core metrics were emitted; five-stage comparison and horizontal trajectory rendered correctly; 9/9 tests passed | Rerun the 50k checkpoint with the existing extended-evaluation command and inspect `direct_vs_algorithm2.md`. |
| 2026-08-25 | Can cross-method outputs preserve the original UIEB image geometry? | Original-size evaluation mode | Real 259×194 UIEB pair with a synthetic smoke checkpoint | `evaluate.py --original-size --batch-size 1`; `ruff check .`; `pytest -q` | 11/11 tests passed; model accepted a non-divisible rectangular input and the five-stage strip contained five 259×194 tiles without square cropping or stretching | Use original-size mode for all future Cold/FlowIE visual comparisons; retain 256×256 only inside the legacy metric calculation. |
| 2026-08-31 | Does a separate natural-image RGB path support a controlled DIV2K saturation ablation without changing the UIEB/Lab experiment? | `natural_rgb_colorization`; DIV2K downloader; saturation 1.0/1.5 configs | Synthetic 8-image natural set for smoke; official DIV2K HR layout contract | `ruff check .`; `python -m pytest -q`; 2-step CPU train; resume to step 4; UIEB-format `evaluate.py` | 15/15 tests passed; training resumed at step 2 and completed step 4; evaluation wrote metrics and RGB trajectory; saturation uses a shared channel-mean gray endpoint and separate checkpoint configs | Ready to run the expanded CUDA smoke test, download DIV2K train/val HR, then train 1.0 before 1.5. Smoke scores are not scientific evidence. |
| 2026-09-02 | What saturation sweep should the natural-image pilot include? | RGB channel-mean saturation preview | Four representative DIV2K validation images | Generate full-resolution 1.0/1.25/1.5/2.0 previews and measure pixels requiring gamut clipping | Clipping varied strongly by scene; maxima among the four previews were 21.7% at 1.25, 26.5% at 1.5, and 49.0% at 2.0 | Keep all four factors as an explicit response-curve/stress-test experiment; do not interpret vividness alone as quality, and preserve other color spaces as a later ablation. |
| 2026-09-02 | Can one command run the complete saturation sweep reproducibly and sequentially? | Four factor-specific configs plus `scripts/train_div2k_4090.sh all` | Config validation, shell dry run, and synthetic natural-image resume smoke | `ruff check .`; `python -m pytest -q`; `DIV2K_DRY_RUN=1 bash scripts/train_div2k_4090.sh all --resume-if-exists --max-steps 100000`; 2-step train followed by `--resume-if-exists --max-steps 4` | 17/17 tests passed; dry run emitted exactly four commands in order 1.0, 1.25, 1.5, 2.0; resume smoke started a missing run at step 0 and resumed the next invocation from step 2 to step 4 | Use `all` for the rented-GPU sweep; runs remain sequential, stop on the first failure, and can resume existing checkpoints while starting not-yet-run factors from step 0. |
