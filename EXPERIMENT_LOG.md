# Experiment log

Record real experiments here. Do not treat smoke tests as scientific evidence.

## 2026-09-04 — RGB factor-1 diagnostic command, existing checkpoint only

- User request: infer with the existing RGB factor-1 model before considering higher saturation. Added `diagnose_rgb_chroma.py` and `bash scripts/diagnose_div2k_rgb_4090.sh`; the existing Lab entry delegates to the same diagnostic implementation with an explicit, checked Lab representation. Training, data transforms, model, configs and normal Algorithm 2 remain unchanged.
- Contract: `outputs/div2k_rgb_sat1_50k/checkpoints/step_050000.pt`, EMA, embedded step 50000, T=20, factor 1; same seed42 DIV2K validation images 0804/0815/0882/0895, original geometry and 512/64 tiling. RGB input/anchor normalization matches training: channel-mean gray and RGB [-1,1], never Lab tensors. Lab conversion is metric-only.
- Starts: t=10/15/18 plus explicit wrapper-enabled t=20 same-weight full-gray control. Output `evaluation/div2k_rgb_sat_1.00x_partial_t20_step050000/`; per-level input/Direct/Algorithm 2 and separate comparison/trajectory folders; source reference and analytic inverse controls. Metadata now also records representation, retention definition, checkpoint SHA256, diagnostic source SHA256 and Git revision. A Git revision alone does not certify a clean working tree or original training provenance.
- Verification: all 76 tests passed, including rectangular CPU end-to-end RGB export, RGB oracle trajectory at t=10/15/18/20, bit-identical full-gray sampler entry, RGB/Lab mismatch rejection, wrong factor/step rejection, original RGB-gray pixel check, matching seed42 subset, and old Lab CLI regression coverage. Ruff, shell syntax and diff whitespace checks passed. Synthetic test weights labeled 50k are fixtures, not evidence of training or restoration quality.
- Status: no trained RGB checkpoint was found in the inspected local repo/Downloads; actual inference must run on the user's instance. No real RGB restoration score is claimed and no new training was launched. No additional saturation models or dataset evaluation were started.

## 2026-09-04 — Existing T=20 model recovers partial-chroma DIV2K inputs

- Question: is the factor-1 DIV2K Lab model intrinsically forced to output brown, or can it recover diverse hues when a/b evidence remains? Result source: `/Users/ed/Downloads/div2k_lab_partial_t20_step050000/`; prior full-gray comparator: `/Users/ed/Downloads/CIE_Lab val/`.
- Run provenance: metadata reports completion, `step_050000.pt` at step 50000, `natural_lab_colorization`, T=20, factor 1, seed42 images 0804/0815/0882/0895, CUDA torch 2.5.1+cu121, original size, tiles 512/overlap64. Command provided: `bash scripts/diagnose_div2k_lab_4090.sh`; local audit code revision `de7c796`. The export does not record the instance's Git revision or checkpoint hash, so identical weight bytes across old/new exports are not independently proven.
- Recorded tensor metrics, equal weighting over four images: at 50%/25%/10% retained a/b, Direct Delta-E76 = 0.8921/1.0129/1.4418; Algorithm 2 = 0.5436/0.9950/1.6025; Algorithm 2 RGB PSNR = 48.7999/43.7900/39.8694 dB. Analytic inversion Delta-E76 is about 1e-5, confirming that this synthetic input retains sufficient target information for an essentially exact nonlearned inverse.
- Independent audit: all 36 new input/direct/prediction PNGs retain their reference geometry (2040-wide, height 1200/1356/1356/1284); all four references exactly match the old export. Using scikit-image D65 Lab on original-size 8-bit PNGs gives Algorithm 2 Delta-E76 = 0.6147/1.0356/1.6241, versus 20.0281 for the same four OLD full-gray predictions. Do not compare this four-image old score to the old Val100 aggregate, or mix PNG scores with pre-quantization tensor scores. Old metadata reports `best.pt` at 50000 and identical tiling/split, not an independently hashed same-weight control.
- Visual evidence: blue rails/denim, pink clothing, green vegetation, yellow mushroom and magenta flowers return, whereas the matched old full-gray images are sepia. Inspection of the t=18 trajectory at spatial stride 8 shows decreasing mean Delta-E76 at every transition for each image (18/18 each); this is a sampled trajectory check, not the repository's PSNR monotonic metric or proof that every pixel improves. Diagnostic previews/audit script are in `/private/tmp/div2k_partial_audit.Je2C75/`; source exports remain unchanged.
- Interpretation: the observed failure is not an unavoidable brown output from this Lab representation/decoder at every timestep. Evidence points toward failure at the no-color-information endpoint, but does not rule out all t=20-specific implementation/training issues. Current training code includes t=20 in its random sampling; it is not omitted. Direct already performs well and has lower mean color error than Algorithm 2 at 10%, so these data do not establish a general benefit from iteration.
- Decision: continue only as a small diagnostic, not a new saturation sweep or longer training run. These four synthetic self-pairs do not demonstrate underwater restoration or semantic colorization: `(a,b)` divided by the known retained fraction already recovers target color. Next discriminate restoration from simple color amplification on a fixed small subset of UIEB Test90, constructing inputs from RAW ONLY and using GT solely for scoring; include raw and analytic chroma scaling controls. Wrong underwater hues may be amplified. A same-checkpoint t=20/optional t=19 rerun can strengthen endpoint attribution if needed before changing training.

## 2026-09-04 — Correction: use the existing T=20 checkpoint for the diagnostic

- The previous T=8-only requirement mixed up a strict UIEB-recipe replication with an inexpensive endpoint-information diagnostic. It was unnecessary for the latter. No new T=8 training is required.
- `bash scripts/diagnose_div2k_lab_4090.sh` now reads `outputs/div2k_lab_sat1_50k/checkpoints/step_050000.pt`, checks against `configs/div2k_lab_sat1_50k.yaml`, preserves T=20, and starts at t=10,15,18 (50%,25%,10% chroma retained). Output: `evaluation/div2k_lab_partial_t20_step050000/`.
- The validator accepts both Lab modes when they match the selected configuration, while still rejecting the wrong experiment, RGB mode, saturation, diffusion schedule or training step. T=8 remains available only through an explicitly selected matching config and starts, not as a prerequisite.
- Added T=20 oracle and full CPU CLI coverage alongside T=8 coverage; all 62 tests, Ruff, shell syntax and wrapper dry run passed. Training/model/sampler code and existing checkpoints remain unchanged. Real GPU diagnosis is pending the user's command on the instance.

## 2026-09-04 — Initial partial-Lab tooling (T=8 restriction superseded above)

- Question: does a DIV2K model preserve/recover correct hues when its input still contains color evidence? The UIEB-style T=8 factor-1 training configuration remains unchanged; this is an intermediate-start inference experiment, not a retrained partial-gray endpoint.
- Deliverable: `diagnose_lab_chroma.py` and `bash scripts/diagnose_div2k_lab_4090.sh`. They require the DIV2K UIEB-style `step_050000.pt` and verify embedded experiment/config/step rather than choosing `best.pt` or substituting the older T=20 model. Checkpoint experiment names remain recorded provenance, not proof of the original training data contents.
- Conditions: fixed seed42 subset of four DIV2K validation images; start t=4,6,7 on the unchanged T=8 Lab path (50%,25%,12.5% a/b retained), saturation factor 1.0. Original geometry and 512/64 tiled inference are constant. Default excludes full gray; an explicit optional flag adds t=8 as a separate control.
- Controls/artifacts: input baseline, Direct, Algorithm 2, analytic inversion, target, actual-t trajectories; per-image PSNR/Delta-E76/chroma/mean-a/b CSV flushed after each image-condition; metadata written at startup. Comparisons and trajectories have separate subfolders. Analytical inversion demonstrates that partial desaturation still contains the answer and is not an underwater/grayscale restoration benchmark.
- Verification: CPU synthetic CLI/oracle tests and the full suite; 56 tests passed, Ruff passed, shell syntax passed. Oracle tests cover original time labels and verify that the new diagnostic entry at t=8 is bit-identical to the existing Algorithm 2. The existing training, bridge, model, and regular evaluation code are not modified.
- Status/decision: no real inference or GPU training has run locally. The required T=8 DIV2K checkpoint's existence on the instance is unconfirmed. If absent, the prerequisite control must be trained explicitly before this strict experiment; do not describe a T=20 run as having identical UIEB settings.

## 2026-09-04 — UIEB retrain evaluation audit (step mismatch; conclusion deferred)

- Sources: `/Users/ed/Downloads/uieb_lab_retrain_test90/` and `/Users/ed/Downloads/cold_gray_original/`; inspected metrics, training summaries, rendered previews, and all exported prediction/reference PNG pairs. Local analysis code version: `64cc3b9`; the remote training code/config/environment are not included in these evaluation folders.
- Both folders contain exactly the same 90 test image names, with identical reference pixels and matching image dimensions. CSV row/preview order is reversed. The different split-file SHA does not indicate different test membership; full train/val manifest equality has not been verified from these outputs.
- Confound: fresh `best.pt` is step 15,000, whereas the old evaluated checkpoint is step 50,000. The fresh training summary confirms training finished at 50,000. The evaluator command previously provided selected `best.pt`, so this is not yet a matched-step regression comparison.
- Provisional observation only: independently computed, equally image-weighted Lab chroma is 9.04 fresh vs 11.29 old (reference 20.61); mean b* is -4.24 vs -5.91, without evidence of a dataset-wide switch to the DIV2K-style warm/brown output. Original-size Delta-E76 is 20.553 vs 19.778; legacy 256-pixel PSNR-RGB is 18.247 vs 18.462. These numbers do not establish a code-regression cause.
- Training summary: validation PSNR peaks near 14k and then plateaus; `best.pt` is selected only at 5k save intervals, explaining the 15k saved checkpoint. Do not relabel it as a 50k checkpoint.
- Decision: no retraining or implementation change yet. Rerun inference with `UIEB_RETRAIN_CHECKPOINT=outputs/uieb_lab_retrain_50k/checkpoints/step_050000.pt UIEB_RETRAIN_EVAL_DIR=evaluation/uieb_lab_retrain_50k_test90 bash scripts/evaluate_uieb_lab_retrain_4090.sh --preview-count 90`, preserving the existing 15k evaluation. Compare images by filename, not preview index.

## 2026-09-04 — Reported 50k export restores the old UIEB appearance

- Source: `/Users/ed/Downloads/uieb_lab_retrain_50k_test90/`, supplied after the explicit `step_050000.pt` command above. At inspection it contains 90 predictions, direct predictions, references, comparisons, and trajectories, plus the training summary, but no evaluation `metrics.json` or metric CSV. The actual inference checkpoint step therefore still needs metadata confirmation; folder name and the 50k training summary alone are not proof.
- Matched-image audit: 90/90 names overlap the old evaluation, every reference pixel is identical, and every prediction/reference geometry matches. New `batch_087.png` corresponds to old `batch_002.png` (449_img_), rather than matching by preview index.
- Independently recomputed on original-size 8-bit PNGs, equally weighting images: old/new RGB PSNR = 18.3803/18.5238 dB; Delta-E76 = 19.7725/19.6060; mean Lab chroma = 11.2933/10.7551 (GT = 20.6113); mean a* = -5.1309/-4.9986 and b* = -5.9141/-5.6555. Do not mix these original-size PNG scores with the legacy 256px RGB metrics or unquantized tensor metrics.
- Paired bootstrap (5,000 resamples, seed 42) of new-minus-old: PSNR +0.1435 dB, 95% interval [0.0493, 0.2423]; Delta-E76 -0.1666, interval [-0.3541, 0.0326]; chroma -0.5381, interval [-0.7494, -0.3451]. These intervals describe sampling variation across these images, not training-seed uncertainty or proof of equivalence.
- Visual checks included the original diver and statue previews, the largest old/new image difference (226_img_), and the diver's trajectory. The new export is cool/desaturated, not globally brown; the diver gradually returns to blue along its trajectory. This weakens the shared-output-sepia-regression hypothesis, but does not establish that every implementation detail is unchanged or explain DIV2K failure.
- Decision: retain the UIEB appearance control; defer a formal matched-50k result until the inference metadata is supplied. Do not scale saturation or train longer merely from this result. Next investigate DIV2K factor-1 color learning with the same paired Lab path.
- User-requested organization: future `evaluate.py` comparisons go into `batches/`, trajectories into `trajectories/`; old downloads are untouched. Only save paths and documentation change, with CPU CLI tests covering Lab, RGB, and one-shot paths and original geometry.

| Date | Question | Code/config | Data | Command | Result | Decision |
|---|---|---|---|---|---|---|
| 2026-08-24 | Does the implementation satisfy endpoint, sampler, and resume invariants? | Initial 50k implementation | Synthetic tensors and 8 synthetic paired images | `pytest -q`; smoke train 2 steps; `--resume auto --max-steps 4`; `evaluate.py` | 4/4 tests passed; CLI resumed from step 2 and completed step 4; evaluation artifacts written | Implementation is ready for a 50-pair overfit test; these results are not scientific evidence. |
| 2026-08-24 | Can a fresh rented 4090 instance reach training with a short reproducible sequence? | CUDA 4090 bootstrap scripts | Local UIEB copy; local CPU used only for preflight | `bash -n scripts/*.sh`; `ruff check .`; `pytest -q`; `tools/prepare_uieb.py --skip-download`; 2-step CPU smoke train | Shell/Python checks passed; UIEB validated as 890 pairs and split 720/80/90 | Push bootstrap changes; the instance must still pass `scripts/smoke_test_cuda.sh` before the paid 50k run. |
| 2026-08-25 | Can evaluation expose convergence and produce inspectable trajectories? | Evaluation report and horizontal visual update | Synthetic 2-step smoke run; layout tensors | `ruff check .`; `pytest -q`; smoke `train.py` then `evaluate.py` | 7/7 tests passed; evaluate produced a 2880×1620 curve report, summary JSON, 2048px comparison strips, horizontal trajectory strips, and metrics | Ready to rerun the real checkpoint at a fixed 512 crop; compare all later checkpoints/baselines with the same evaluation size. |
| 2026-08-25 | Can the fixed Test 90 use the same evaluation suite as prior FlowIE runs? | FlowIE-compatible extended evaluator | Test filenames compared with the previous FlowIE seed-42 list; synthetic paired metric test | `ruff check .`; `pytest -q` | Test filenames matched 90/90; 8/8 tests passed; evaluator writes the 14 legacy metrics plus Delta-E76 and trajectory monotonic as per-image CSV and mean Markdown | Rerun the real checkpoint with `--extended-metrics --extended-metric-size 256`; use identical spatial preprocessing for strict cross-model claims. |
| 2026-08-25 | Does Algorithm 2 improve over direct reconstruction from the same Cold model? | Same-checkpoint Direct-vs-Algorithm-2 evaluator | Two-image synthetic paired smoke set | 2-step CPU train; `evaluate.py`; `ruff check .`; `pytest -q` | Direct and Algorithm 2 core metrics were emitted; five-stage comparison and horizontal trajectory rendered correctly; 9/9 tests passed | Rerun the 50k checkpoint with the existing extended-evaluation command and inspect `direct_vs_algorithm2.md`. |
| 2026-08-25 | Can cross-method outputs preserve the original UIEB image geometry? | Original-size evaluation mode | Real 259×194 UIEB pair with a synthetic smoke checkpoint | `evaluate.py --original-size --batch-size 1`; `ruff check .`; `pytest -q` | 11/11 tests passed; model accepted a non-divisible rectangular input and the five-stage strip contained five 259×194 tiles without square cropping or stretching | Use original-size mode for all future Cold/FlowIE visual comparisons; retain 256×256 only inside the legacy metric calculation. |
| 2026-08-31 | Does a separate natural-image RGB path support a controlled DIV2K saturation ablation without changing the UIEB/Lab experiment? | `natural_rgb_colorization`; DIV2K downloader; saturation 1.0/1.5 configs | Synthetic 8-image natural set for smoke; official DIV2K HR layout contract | `ruff check .`; `python -m pytest -q`; 2-step CPU train; resume to step 4; UIEB-format `evaluate.py` | 15/15 tests passed; training resumed at step 2 and completed step 4; evaluation wrote metrics and RGB trajectory; saturation uses a shared channel-mean gray endpoint and separate checkpoint configs | Ready to run the expanded CUDA smoke test, download DIV2K train/val HR, then train 1.0 before 1.5. Smoke scores are not scientific evidence. |
| 2026-09-02 | What saturation sweep should the natural-image pilot include? | RGB channel-mean saturation preview | Four representative DIV2K validation images | Generate full-resolution 1.0/1.25/1.5/2.0 previews and measure pixels requiring gamut clipping | Clipping varied strongly by scene; maxima among the four previews were 21.7% at 1.25, 26.5% at 1.5, and 49.0% at 2.0 | Keep all four factors as an explicit response-curve/stress-test experiment; do not interpret vividness alone as quality, and preserve other color spaces as a later ablation. |
| 2026-09-02 | Can one command run the complete saturation sweep reproducibly and sequentially? | Four factor-specific configs plus `scripts/train_div2k_4090.sh all` | Config validation, shell dry run, and synthetic natural-image resume smoke | `ruff check .`; `python -m pytest -q`; `DIV2K_DRY_RUN=1 bash scripts/train_div2k_4090.sh all --resume-if-exists --max-steps 100000`; 2-step train followed by `--resume-if-exists --max-steps 4` | 17/17 tests passed; dry run emitted exactly four commands in order 1.0, 1.25, 1.5, 2.0; resume smoke started a missing run at step 0 and resumed the next invocation from step 2 to step 4 | Use `all` for the rented-GPU sweep; runs remain sequential, stop on the first failure, and can resume existing checkpoints while starting not-yet-run factors from step 0. |
| 2026-09-03 | Is the warm RGB result caused by the paper-style RGB colorization representation? | `natural_lab_colorization`; `configs/div2k_lab_sat1_50k.yaml` | Synthetic 8-image natural set; same factor-1 RGB config used as the control contract | `ruff check .`; `python -m pytest -q`; 2-step CPU train; resume to step 4; paired original-size evaluation | 22/22 tests passed; the Lab config differs from RGB factor 1 only in mode/experiment/output; training resumed at step 2 and evaluation read the Lab checkpoint and emitted Direct, Algorithm 2, trajectory, and metrics | Run the single 50k Lab control, then compare it with RGB factor 1 on full DIV2K validation and the unchanged UIEB Test 90 before changing the loss or data. Smoke scores are not scientific evidence. |
| 2026-09-03 | Is the brown output already present in-domain, rather than caused by UIEB domain shift? | Full-resolution tiled evaluator; fixed DIV2K Val100 manifest | Synthetic rectangular images for implementation smoke; official DIV2K validation filenames as the real-run contract | `python -m pytest -q`; `ruff check .`; 2-step Lab CPU train followed by original-size tiled evaluation | 28/28 tests passed before the manifest assertion was added; the tiled path preserved rectangular geometry and completed Direct plus Algorithm 2; no real checkpoint result was produced locally | Run Lab and RGB factor 1 on all DIV2K Val100. If both are brown in-domain, stop attributing the failure to UIEB and debug training/state conversion before continuing to 100k. |
| 2026-09-03 | Did later changes break the original UIEB Lab training path? | Fresh UIEB regression config plus fixed 1k validation previews; UIEB-style DIV2K gate | Synthetic paired smoke data only; the real UIEB/DIV2K runs remain pending | Config invariance test; shell dry runs; 2-step CPU paired training; `python -m pytest -q`; `ruff check .` | Original `cold_gray_50k.yaml` SHA remains unchanged; fresh script has no resume argument; 37 tests passed before the named-preview smoke rerun; synthetic training saved validation samples | Retrain UIEB from step zero. If it becomes brown, stop as a shared regression; otherwise run only DIV2K factor 1 and inspect 1k previews before starting higher saturation factors. |
